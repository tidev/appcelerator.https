#!groovy
library 'pipeline-library'

buildModule {
	sdkVersion = '9.2.0.v20200914000026' // use a master build with ARM64 support
	npmPublish = false // By default it'll do github release on master anyways too
	iosLabels = 'osx && xcode-12'
}
